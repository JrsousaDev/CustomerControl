import { CSSProp } from "styled-components";

export interface IDefaultDashboardProps {
  title: string;
  values: any;
  bgColor: CSSProp;
  children: any;
}

export interface IBadgeProps {
  bgColor: CSSProp;
}